<div class="container-fluid">
  <% @posts.each do |post| %>
    <div class="row">
        <div class="jumbotron">
          <div class="page-header">
            <h1><%= post.title %> <small>by: <%= post.user.email %></small></h1>
          </div>
        </div>
    </div>

  <% end %>
</div>
